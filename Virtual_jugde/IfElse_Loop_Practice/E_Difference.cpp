#include<bits/stdc++.h>
using namespace std;
int main()
{
    long long int a, b, c, d;
    cin >> a >> b >> c >> d;
    long long int difference = (a * b) - (c * d);

    cout << "Difference = " << difference << endl;
    return 0;
}